// ACTION. Describes what it is you want to do
// action increment
export const increment = () => {
   return {
      type: 'INCREMENT',
   }
};

// action decrement
export const decrement = () => {
   return {
      type: 'DECREMENT',
   }
};